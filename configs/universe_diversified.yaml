# Universe Diversified: Sector-diversified expansion (A1)
# Base: 7 existing tickers from phase3_best.yaml
# Added: COST (Consumer Defensive) — passed evaluation with 64.9% accuracy at conf 55%+
#
# Evaluation results (2026-04-03, 3-fold WF, step=126):
#   COST: 64.9% conf55+ acc (37 trades) — PASS
#   PG:   42.4% conf55+ acc — FAIL
#   LLY:  50.0% conf55+ acc — FAIL (borderline)
#   JNJ:  44.4% conf55+ acc — FAIL
#   XOM:  50.0% conf55+ acc — FAIL (borderline)
#   CAT:  28.9% conf55+ acc — FAIL
#
# Sectors covered:
#   Tech:      MSFT, GOOGL, NVDA, META
#   Consumer:  AMZN, COST (new)
#   Finance:   JPM, V

labels:
  horizon: 10
  direction_threshold: 0.03

features:
  tiers: [1, 2, 3, 4, 5]
  max_features: 50
  selected:
    - yield_10y
    - yield_10y_change_21d
    - price_to_52w_low
    - vix_level
    - sp500_dist_sma50
    - dist_sma200
    - sp500_ret_21d
    - vix_change_5d
    - atr_14
    - realized_vol_63d
    - dist_sma50
    - realized_vol_21d
    - macd_signal
    - eps_revision_7d
    - roc_20
    - range_52w_position
    - max_drawdown_63d
    - ret_63d
    - lower_shadow_ratio
    - ret_5d

universe:
  tickers:
    # Tech (existing)
    - "MSFT"
    - "GOOGL"
    - "NVDA"
    - "META"
    # Consumer (existing + new)
    - "AMZN"
    - "COST"      # New: Costco — Consumer Defensive, 64.9% conf55+ accuracy
    # Finance (existing)
    - "JPM"
    - "V"

backtest:
  confidence_threshold: 0.55
